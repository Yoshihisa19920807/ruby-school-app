.card
  .card-body
    = comment.content
  .card-footer
    = comment.user
    = comment.created_at
  .carf-footer
    = link_to "edit", edit_course_lesson_comment_path(@course, @lesson, comment), class: "btn btn-sm btn-warning my-1"