%nav.navbar.navbar-expand-lg.navbar-dark.bg-dark
  -# enable home button
  %a.navbar-brand{:href => "/"} Rubygems
  %button.navbar-toggler{"aria-controls" => "navbarSupportedContent", "aria-expanded" => "false", "aria-label" => "Toggle navigation", "data-bs-target" => "#navbarSupportedContent", "data-bs-toggle" => "collapse", :type => "button"}
    %span.navbar-toggler-icon
  #navbarSupportedContent.collapse.navbar-collapse
    - if  current_user
      %ul.navbar-nav.me-auto
        %li.nav-item{class: "#{'active fw-bold' if current_page?(root_path)}"}
          = link_to root_path, class: 'nav-link' do
            .fa.fa-home
            Home
        %li.nav-item{class: "#{'active fw-bold' if current_page?(courses_path)}"}
          = link_to courses_path, class: 'nav-link' do
            .fa.fa-graduation-cap
            Courses
        - if current_user.has_role?(:admin) || current_user.has_role?(:student)
          %li.nav-item{class: "#{'active fw-bold' if current_page?(purchased_courses_path)}"}
            = link_to purchased_courses_path, class: 'nav-link' do
              .fa.fa-wallet
              Purchased Courses
          %li.nav-item{class: "#{'active fw-bold' if current_page?(review_pending_courses_path)}"}
            = link_to review_pending_courses_path, class: 'nav-link' do
              .fa.fa-comment-dots
              Review Pending Courses
        - if current_user.has_role?(:admin) || current_user.has_role?(:teacher)
          %li.nav-item{class: "#{'active fw-bold' if current_page?(created_courses_path)}"}
            = link_to created_courses_path, class: 'nav-link' do
              .fa.fa-hammer
              Created Courses
          %li.nav-item{class: "#{'active fw-bold' if current_page?(new_course_path)}"}
            = link_to new_course_path, class: 'nav-link' do
              .fa.fa-plus
              New Course
        - if current_user.has_role?(:admin)
          %li.nav-item{class: "#{'active fw-bold' if current_page?(enrollments_path)}"}
            = link_to enrollments_path, class: 'nav-link' do
              .fas.fa-sign-in-alt
              Enrollments
          %li.nav-item{class: "#{'active fw-bold' if current_page?(users_path)}"}
            = link_to users_path, class: 'nav-link' do
              .fa.fa-users
              Users
      -# .form-inline.my-2.my-lg-0
      -#   =form_tag(courses_path, method: :get) do
      -#     .input-group
      -#       = text_field_tag :title, params[:title], autocomplete: 'off', placeholder: "Search", class: 'form-control-sm'
      -#       %span.input-group-append
      -#         %button.btn.btn-primary.btn-sm{:type => "submit"}
      -#           %span.fa.fa-search{"aira-hidden" => "submit"}
      .form-inline.my-2.my-lg-0.col-2
        = search_form_for @courses_ransack, as: :search_courses, url: courses_path do |f|
          .input-group
            = f.search_field :title_cont, style: 'float: left;', placeholder: 'Title', class: 'form-control form-control-sm'
            %span.input-group-append
              %button.btn.btn-primary.btn-sm{:type => "submit"}
                %span.fa.fa-search{"aira-hidden" => "true"}
      %ul.navbar-nav.me-auto
        - if current_user.has_role?(:admin)
          %li.nav-item{class: "#{'active fw-bold' if current_page?(home_activity_path)}"}
            = link_to home_activity_path, class: 'nav-link' do
              .fa.fa-bell
              Activity log
        %li.nav-item.dropdown
          -#%a#navbarDropdown.nav-link.dropdown-toggle{"aria-expanded" => "false", "aria-haspopup" => "true", "data-toggle" => "dropdown", :href => "#", :role => "button"}
          %a#navbarDropdown.nav-link.dropdown-toggle{"aria-expanded" => "false", "aria-haspopup" => "true","data-bs-toggle" => "dropdown", :href => "#", :role => "button"}
            %b= current_user.email
            -#Dropdown
          .dropdown-menu.dropdown-menu-right{"aria-labelledby" => "navbarDropdown"}
            = link_to user_path(current_user), class: "dropdown-item" do
              .fa.fa-user
              %b My profile
            = link_to edit_user_registration_path, class: "dropdown-item" do
              .fa.fa-cog
              %b Account settings
            = link_to destroy_user_session_path, method: :delete, class: "dropdown-item" do
              .fa.fa-sign-out-alt
              %b Sign out
    - else
      %ul.navbar-nav.me-auto
        %li.nav-item= link_to 'Sign Up', new_user_registration_path, class: 'nav-link'
        %li.nav-item= link_to 'Log In', new_user_session_path, class: 'nav-link'